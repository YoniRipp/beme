# BMe Frontend - Environment Variables
# Copy to .env.local and fill in values

# API URL (leave empty for same-origin in production)
VITE_API_URL=

# Google OAuth Client ID
VITE_GOOGLE_CLIENT_ID=

# Feature Flags (set to 'true' to enable)
VITE_FF_PWA_ENABLED=false
VITE_FF_PWA_OFFLINE_SYNC=false
VITE_FF_PWA_PUSH=false

# Push Notifications (VAPID public key from backend, required when PWA_PUSH=true)
VITE_VAPID_PUBLIC_KEY=
