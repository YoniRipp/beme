// Domain types now live in @trackvibe/shared so both clients share one definition.
// This module stays as a re-export so existing import sites keep working.
export * from '@trackvibe/shared/settings';
