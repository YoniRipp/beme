// Form schemas now live in @trackvibe/shared so both clients validate identically.
// This module stays as a re-export so existing import sites keep working.
export * from '@trackvibe/shared/schemas';
