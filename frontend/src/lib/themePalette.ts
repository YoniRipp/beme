// The accent palette now lives in @trackvibe/shared (alongside its hex-derived
// counterpart for React Native) so both clients share one definition. This module
// stays as a re-export so existing import sites keep working.
export * from '@trackvibe/shared/settings';
